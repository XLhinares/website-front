import "package:flutter/widgets.dart";

/// An expandable card
class ContactExpandableCard extends StatelessWidget {
  // VARIABLES =================================================================

  // CONSTRUCTOR ===============================================================

  /// Returns a [ContactExpandableCard] instance.
  const ContactExpandableCard({super.key});

  // BUILD =====================================================================
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }

  // METHODS ===================================================================
}
